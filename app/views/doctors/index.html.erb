<h1>Tony's Private Clinic for Mutants</h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li><%= link_to "#{doctor.first_name} #{doctor.last_name} (#{doctor.speciality})", doctor_path(doctor) %></li>
  <% end %>
</ul>


<%= link_to "Add a new doctor", new_doctor_path %>
